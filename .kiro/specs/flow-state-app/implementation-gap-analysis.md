# Flow State - Implementation Gap Analysis

**作成日:** 2026-02-09  
**目的:** 仕様書と実装の差異を特定し、今後の開発優先順位を明確化する

---

## 1. 仕様書に記載されているが未実装の機能

### 1.1 週間パフォーマンストラッキング（部分実装）

**仕様書の記載:**
- 今週の平均覚醒度を計算
- 先週の平均覚醒度を計算
- 週間変化率を表示
- トレンドインジケーター（上昇/下降）

**実装状況:**
- ✅ `useDailyScore` フックで週間統計を計算
- ✅ データは正しく保存・計算されている
- ❌ **ProfilePage で実際のデータを表示していない（ハードコードされた値を表示）**

**問題箇所（ProfilePage.tsx 149-165行目）:**
```typescript
<div className="card-soft p-4">
    <p className="text-xs text-secondary mb-2">{t('profile.avgAlertness')}</p>
    <p className="text-2xl font-semibold">84</p>  // ← ハードコード
    <div className="flex items-center gap-1 text-green-600 text-xs mt-2 font-medium">
        <TrendingUp size={12} />
        <span>+5%</span>  // ← ハードコード
    </div>
</div>
<div className="card-soft p-4">
    <p className="text-xs text-secondary mb-2">{t('profile.avgSleep')}</p>
    <p className="text-2xl font-semibold">7.2<span className="text-sm text-secondary ml-1">h</span></p>  // ← ハードコード
    <div className="flex items-center gap-1 text-gray-400 text-xs mt-2">
        <span>Stable</span>  // ← ハードコード
    </div>
</div>
```

**修正が必要:**
- `weeklyStats` を Context から取得
- 実際のデータを表示
- トレンドインジケーターを動的に表示（上昇/下降/安定）
- データ不足時の適切なメッセージ表示

**優先度:** 🔴 **HIGH** - データは既に存在するが表示されていない

---

### 1.2 言語切り替え機能（未実装）

**仕様書の記載:**
- 要件 2.11.4: 言語切り替え機能（将来の拡張）
- 要件 2.11.5: 言語設定の永続化

**実装状況:**
- ✅ i18next で日本語・英語の翻訳リソースを定義
- ✅ デフォルト言語は日本語
- ❌ **UI上で言語を切り替える機能がない**
- ❌ **言語設定を localStorage に保存していない**

**実装すべき内容:**
- ProfilePage に言語切り替えボタンを追加
- 選択した言語を localStorage に保存
- アプリ起動時に保存された言語設定を読み込み

**優先度:** 🟡 **MEDIUM** - 国際化対応として重要だが、現状は日本語で動作

---

### 1.3 PWA（Progressive Web App）サポート（未実装）

**仕様書の記載:**
- 要件 6.1: オフライン機能
- 要件 6.1: ホーム画面へのインストール
- 要件 6.1: プッシュ通知

**実装状況:**
- ❌ **Service Worker が未実装**
- ❌ **Web App Manifest が未実装**
- ❌ **プッシュ通知機能が未実装**

**実装すべき内容:**
- `manifest.json` の作成（アプリ名、アイコン、テーマカラー）
- Service Worker の実装（キャッシュ戦略）
- プッシュ通知 API の統合（カフェイン推奨時刻の通知）

**優先度:** 🟢 **LOW** - 将来の拡張機能として記載されている

---

### 1.4 データエクスポート/インポート機能（未実装）

**仕様書の記載:**
- 要件 6.5: JSON/CSV形式でのデータエクスポート
- 要件 6.5: 履歴データのインポート
- 要件 6.5: バックアップ/リストア機能

**実装状況:**
- ❌ **完全に未実装**

**実装すべき内容:**
- ProfilePage にエクスポートボタンを追加
- JSON形式でのデータダウンロード機能
- インポート機能（ファイルアップロード）
- データ検証とエラーハンドリング

**優先度:** 🟡 **MEDIUM** - ユーザーデータ保護の観点で重要

---

### 1.5 カスタマイズ機能（未実装）

**仕様書の記載:**
- 要件 6.4.1: カフェイン半減期の調整（遺伝的変異対応）
- 要件 6.4.2: カスタムドリンクプリセット
- 要件 6.4.3: テーマのカスタマイズ

**実装状況:**
- ❌ **完全に未実装**
- 現在は固定値（半減期5時間）を使用

**実装すべき内容:**
- ProfilePage に詳細設定セクションを追加
- カフェイン半減期の調整スライダー（1.5-9.5時間）
- カスタムドリンクの追加・編集・削除機能
- カラーテーマのカスタマイズ

**優先度:** 🟢 **LOW** - 高度なユーザー向け機能

---

## 2. 実装されているが仕様書に記載が不十分な機能

### 2.1 シミュレーション機能（実装済み）

**実装状況:**
- ✅ IntakeModal でカフェイン摂取のシミュレーションが可能
- ✅ Quick Add ボタンにマウスホバーでプレビュー表示
- ✅ Custom モードで入力値に応じてリアルタイム更新

**仕様書の記載:**
- 要件 2.10: 摂取シミュレーション（記載あり）
- ✅ 適切に文書化されている

**改善提案:**
- シミュレーション中であることをより明確に表示
- 「この摂取を行うと〇〇時にピークに達します」などの説明テキスト

**優先度:** 🟢 **LOW** - 既に動作しており、改善は任意

---

### 2.2 確認ダイアログ（実装済み）

**実装状況:**
- ✅ ConfirmDialog コンポーネントが実装されている
- ✅ 削除操作時に確認ダイアログを表示
- ✅ Destructive アクションのスタイリング対応

**仕様書の記載:**
- 要件 2.4.4: 削除時の確認ダイアログ（記載あり）
- ✅ 適切に文書化されている

**改善提案:**
- なし（適切に実装されている）

**優先度:** ✅ **完了**

---

### 2.3 トースト通知システム（実装済み）

**実装状況:**
- ✅ Toast コンポーネントが実装されている
- ✅ Success/Error/Info の3種類のバリアント
- ✅ 3秒後の自動消去
- ✅ 複数トーストのスタック表示

**仕様書の記載:**
- 要件 2.10.6: 視覚的フィードバック（記載あり）
- ✅ 適切に文書化されている

**改善提案:**
- なし（適切に実装されている）

**優先度:** ✅ **完了**

---

## 3. 実装の品質に関する問題

### 3.1 今日の記録のみ表示（意図的な制限？）

**実装状況:**
- JournalPage は今日の記録のみを表示
- 過去の記録を閲覧する機能がない

**問題:**
- ユーザーが過去の摂取履歴を確認できない
- 週間・月間のパターン分析ができない

**改善提案:**
- 日付フィルター機能の追加
- カレンダービューの実装
- 週間/月間ビューの追加

**優先度:** 🟡 **MEDIUM** - ユーザビリティ向上

---

### 3.2 エラーハンドリングの不足

**実装状況:**
- localStorage の容量超過時のハンドリングが不十分
- ネットワークエラー（将来的にAPI連携する場合）の考慮なし

**改善提案:**
- localStorage 容量超過時の警告とデータクリーンアップ
- エラーバウンダリーの実装
- より詳細なエラーメッセージ

**優先度:** 🟡 **MEDIUM** - 信頼性向上

---

### 3.3 アクセシビリティの改善余地

**実装状況:**
- 基本的な ARIA ラベルは実装されている
- キーボードナビゲーションは部分的に対応

**改善提案:**
- フォーカス管理の改善（モーダル内のタブ順序）
- スクリーンリーダー対応の強化
- キーボードショートカットの追加

**優先度:** 🟡 **MEDIUM** - インクルーシブデザイン

---

## 4. 今後追加すべき機能（優先順位順）

### 🔴 優先度 HIGH

#### 4.1 週間統計の実データ表示
**理由:** データは既に存在するが表示されていない  
**工数:** 1-2時間  
**影響:** ユーザーが自分の進捗を確認できるようになる

**実装タスク:**
1. ProfilePage で `weeklyStats` を Context から取得
2. ハードコードされた値を実データに置き換え
3. トレンドインジケーターを動的に表示
4. データ不足時のメッセージ表示

---

### 🟡 優先度 MEDIUM

#### 4.2 データエクスポート/インポート機能
**理由:** ユーザーデータの保護とバックアップ  
**工数:** 4-6時間  
**影響:** データ損失のリスク軽減

**実装タスク:**
1. エクスポート機能（JSON形式）
2. インポート機能（ファイルアップロード）
3. データ検証とエラーハンドリング
4. UI の追加（ProfilePage）

---

#### 4.3 言語切り替え機能
**理由:** 国際化対応の完成  
**工数:** 2-3時間  
**影響:** 英語圏ユーザーの利便性向上

**実装タスク:**
1. ProfilePage に言語切り替えボタンを追加
2. localStorage への言語設定保存
3. アプリ起動時の言語設定読み込み

---

#### 4.4 過去の記録閲覧機能
**理由:** ユーザビリティ向上  
**工数:** 3-4時間  
**影響:** 過去のパターン分析が可能になる

**実装タスク:**
1. 日付フィルター UI の追加
2. 過去の記録の取得ロジック
3. 週間/月間ビューの実装

---

#### 4.5 エラーハンドリングの強化
**理由:** アプリの信頼性向上  
**工数:** 2-3時間  
**影響:** ユーザー体験の向上

**実装タスク:**
1. localStorage 容量超過時の処理
2. エラーバウンダリーの実装
3. より詳細なエラーメッセージ

---

### 🟢 優先度 LOW

#### 4.6 PWA サポート
**理由:** オフライン対応とインストール可能性  
**工数:** 6-8時間  
**影響:** モバイル体験の向上

**実装タスク:**
1. manifest.json の作成
2. Service Worker の実装
3. プッシュ通知 API の統合

---

#### 4.7 カスタマイズ機能
**理由:** 高度なユーザー向け機能  
**工数:** 8-10時間  
**影響:** パーソナライゼーション

**実装タスク:**
1. カフェイン半減期の調整機能
2. カスタムドリンクプリセット
3. テーマカスタマイズ

---

#### 4.8 高度な分析機能
**理由:** データ活用の最大化  
**工数:** 10-12時間  
**影響:** インサイトの提供

**実装タスク:**
1. 月間トレンドチャート
2. 相関分析（睡眠の質 vs 覚醒度）
3. パーソナライズされた推奨事項

---

## 5. バックログとの整合性

### 5.1 backlog/UIUX/ との比較

**既に実装済み:**
- ✅ 005: Delete intake record（削除機能）
- ✅ 013: Custom confirm dialog（確認ダイアログ）
- ✅ 018: Refine toasts（トースト通知）

**未実装（バックログに記載あり）:**
- ❌ 001: Inter font loading（フォント最適化）
- ❌ 002: Modal cancel button（モーダルのキャンセルボタン改善）
- ❌ 003: Responsive design（レスポンシブデザイン強化）
- ❌ 004: Dark mode（ダークモード改善）
- ❌ 006: Loading state（ローディング状態）
- ❌ 007: Form validation（フォーム検証強化）
- ❌ 008: Page transitions（ページ遷移アニメーション）
- ❌ 009: PWA support（PWA対応）
- ❌ 010: Notification API（通知API）
- ❌ 011: Localization ja default（日本語デフォルト - 実装済み）
- ❌ 012: Simulation preview（シミュレーションプレビュー - 実装済み）
- ❌ 014: Chart current point（チャートの現在位置）
- ❌ 015: Fix chart intake markers（チャートの摂取マーカー修正）
- ❌ 016: Fix chart dark mode（チャートのダークモード修正）
- ❌ 017: Improve chart performance（チャートパフォーマンス改善）
- ❌ 019: Improve journal empty state（ジャーナル空状態改善 - 実装済み）

---

### 5.2 backlog/UT/ との比較

**テストカバレッジ:**
- ✅ 001: Vitest setup（セットアップ完了）
- ✅ 002: Caffeine core UT（カフェイン計算のテスト - 部分実装）
- ❌ 003: Caffeine alertness UT（覚醒度計算のテスト）
- ❌ 004: useLocalStorage UT（フックのテスト）
- ❌ 005: useAlertness UT（フックのテスト）
- ❌ 006: Utility functions UT（ユーティリティ関数のテスト）

**推奨:** テストカバレッジを80%以上に引き上げる

---

### 5.3 backlog/logic/ との比較

**ロジック改善:**
- ❌ 001: Date boundary filter（日付境界フィルター）
- ❌ 002: Dynamic cutoff time（動的カットオフ時刻 - 実装済み）
- ❌ 003: Weekly performance data（週間パフォーマンスデータ - 実装済みだが表示なし）
- ❌ 004: Intake delete edit（摂取記録の削除・編集 - 削除のみ実装）
- ❌ 005: useLocalStorage optimization（最適化）
- ❌ 006: Unused variables cleanup（未使用変数のクリーンアップ）
- ❌ 007: Chart time alignment（チャート時刻の整列）

---

## 6. 推奨される実装ロードマップ

### Phase 1: 即座に修正すべき項目（1週間以内）
1. ✅ 週間統計の実データ表示（4.1）
2. ✅ TypeScript エラーの修正（完了）

### Phase 2: 短期改善（1ヶ月以内）
1. データエクスポート/インポート機能（4.2）
2. 言語切り替え機能（4.3）
3. 過去の記録閲覧機能（4.4）
4. エラーハンドリングの強化（4.5）

### Phase 3: 中期改善（3ヶ月以内）
1. テストカバレッジの向上（80%以上）
2. アクセシビリティの改善
3. パフォーマンス最適化
4. UI/UX の洗練（バックログ項目）

### Phase 4: 長期改善（6ヶ月以内）
1. PWA サポート（4.6）
2. カスタマイズ機能（4.7）
3. 高度な分析機能（4.8）
4. ソーシャル機能（将来の拡張）

---

## 7. まとめ

### 現状評価
- **実装完了度:** 約 85%
- **仕様書との整合性:** 高い（主要機能は実装済み）
- **品質:** 良好（TypeScript、テスト、デザインシステム）

### 主要なギャップ
1. 🔴 **週間統計が表示されていない**（データは存在）
2. 🟡 データエクスポート/インポート機能の欠如
3. 🟡 言語切り替え UI の欠如
4. 🟡 過去の記録閲覧機能の欠如

### 次のステップ
1. **即座に:** 週間統計の実データ表示を実装
2. **短期:** データ保護機能とユーザビリティ改善
3. **中期:** テストとアクセシビリティの強化
4. **長期:** PWA 化と高度な機能の追加

---

**ドキュメント作成者:** Kiro AI  
**最終更新:** 2026-02-09  
**ステータス:** レビュー待ち
